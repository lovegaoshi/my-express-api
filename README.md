This is a collection of ytbi.js parsed and cached players updated on a daily basis. parsing a player costs ~30s and blocks UI on a Samsung S21 so caching it elsewhere is much more effective.

Alternatively a live parser can be deployed on vercel (main but cant get it working) or netlify (the netlify branch). 
